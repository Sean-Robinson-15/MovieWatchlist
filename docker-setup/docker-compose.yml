services:
  app:
    image: ghcr.io/sean-robinson-15/moviewatchlist:latest
    container_name: moviewatchlist
    ports:
      - "8002:8000"

    env_file:
      - ../.env

    volumes:
      - moviewatchlist_storage:/app/storage

    restart: unless-stopped

volumes:
  moviewatchlist_storage: